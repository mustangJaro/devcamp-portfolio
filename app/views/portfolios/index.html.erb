<div class="album py-5 bg-light">
  <div class="container">
  	<%= link_to "Create New Portfolio Item", new_portfolio_path, class: "btn btn-primary" if logged_in?(:site_admin) %>
    <div class="row sortable">
			<%= render partial: 'portfolio_item', collection: @portfolio_items %>
		</div>
	</div>
</div>
  